-- ----------------------------
-- 企业全面预算管理系统 - MySQL 建表脚本
-- 版本: v9.0
-- 日期: 2026-05-30
-- ----------------------------

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- ----------------------------
-- 1、预算科目表（分层级结构）
-- ----------------------------
DROP TABLE IF EXISTS `budget_subject`;
CREATE TABLE `budget_subject` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `subject_code` VARCHAR(20) NOT NULL COMMENT '科目编码（唯一，格式：一级1001，二级1001001）',
  `subject_name` VARCHAR(100) NOT NULL COMMENT '科目名称',
  `subject_type` VARCHAR(20) NOT NULL DEFAULT 'EXPENSE' COMMENT '科目类型（INCOME-收入类，EXPENSE-费用类，ASSET-资产类，LIABILITY-负债类，EQUITY-权益类，COST-成本类）',
  `parent_id` BIGINT(20) DEFAULT 0 COMMENT '父级科目ID（顶级为0）',
  `parent_code` VARCHAR(20) DEFAULT '' COMMENT '父级科目编码（冗余字段，便于查询）',
  `level` INT(1) NOT NULL DEFAULT 1 COMMENT '科目层级（1-一级科目，2-二级科目，3-三级科目）',
  `ancestors` VARCHAR(500) DEFAULT '' COMMENT '祖级列表（如：0/1001/1001001）',
  `is_leaf` TINYINT(1) DEFAULT 0 COMMENT '是否叶子节点（0-否有子科目，1-是无子科目）',
  `sort_order` INT(4) DEFAULT 0 COMMENT '排序号（同级科目内的显示顺序）',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用（0-禁用，1-启用）',
  `description` VARCHAR(500) DEFAULT NULL COMMENT '科目说明',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者',
  `create_time` DATETIME DEFAULT NULL COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者',
  `update_time` DATETIME DEFAULT NULL COMMENT '更新时间',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_subject_code` (`subject_code`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_level` (`level`),
  KEY `idx_ancestors` (`ancestors`(100)),
  KEY `idx_subject_type` (`subject_type`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算科目表';

-- ----------------------------
-- 初始化-预算科目表数据（示例数据）
-- ----------------------------
INSERT INTO `budget_subject` VALUES 
-- 收入类科目 (INCOME)
(1, '1001', '营业收入', 'INCOME', 0, '', 1, '0', 0, 1, 1, '企业主营业务收入', 'admin', NOW(), '', NULL, NULL),
(2, '1001001', '主营业务收入', 'INCOME', 1, '1001', 2, '0/1001', 0, 1, 1, '主要业务产生的收入', 'admin', NOW(), '', NULL, NULL),
(3, '1001001001', '产品销售收入', 'INCOME', 2, '1001001', 3, '0/1001/1001001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(4, '1001001002', '服务收入', 'INCOME', 2, '1001001', 3, '0/1001/1001001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(5, '1001002', '其他业务收入', 'INCOME', 1, '1001', 2, '0/1001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(6, '1002', '投资收益', 'INCOME', 0, '', 1, '0', 0, 2, 1, '对外投资取得的收益', 'admin', NOW(), '', NULL, NULL),
(7, '1002001', '股权投资收益', 'INCOME', 6, '1002', 2, '0/1002', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(8, '1002002', '债权投资收益', 'INCOME', 6, '1002', 2, '0/1002', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),

-- 成本类科目 (COST)
(9, '2001', '营业成本', 'COST', 0, '', 1, '0', 0, 1, 1, '企业主营业务成本', 'admin', NOW(), '', NULL, NULL),
(10, '2001001', '主营业务成本', 'COST', 9, '2001', 2, '0/2001', 0, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(11, '2001001001', '直接材料成本', 'COST', 10, '2001001', 3, '0/2001/2001001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(12, '2001001002', '直接人工成本', 'COST', 10, '2001001', 3, '0/2001/2001001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(13, '2001001003', '制造费用', 'COST', 10, '2001001', 3, '0/2001/2001001', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(14, '2002', '其他业务成本', 'COST', 0, '', 1, '0', 0, 2, 1, '其他业务活动发生的成本', 'admin', NOW(), '', NULL, NULL),

-- 费用类科目 (EXPENSE)
(15, '3001', '销售费用', 'EXPENSE', 0, '', 1, '0', 0, 1, 1, '销售过程中发生的费用', 'admin', NOW(), '', NULL, NULL),
(16, '3001001', '广告费', 'EXPENSE', 15, '3001', 2, '0/3001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(17, '3001002', '运输费', 'EXPENSE', 15, '3001', 2, '0/3001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(18, '3001003', '销售人员薪酬', 'EXPENSE', 15, '3001', 2, '0/3001', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(19, '3001004', '业务招待费', 'EXPENSE', 15, '3001', 2, '0/3001', 1, 4, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(20, '3002', '管理费用', 'EXPENSE', 0, '', 1, '0', 0, 2, 1, '企业管理活动发生的费用', 'admin', NOW(), '', NULL, NULL),
(21, '3002001', '办公费', 'EXPENSE', 20, '3002', 2, '0/3002', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(22, '3002002', '差旅费', 'EXPENSE', 20, '3002', 2, '0/3002', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(23, '3002003', '管理人员薪酬', 'EXPENSE', 20, '3002', 2, '0/3002', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(24, '3002004', '折旧费', 'EXPENSE', 20, '3002', 2, '0/3002', 1, 4, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(25, '3002005', '咨询费', 'EXPENSE', 20, '3002', 2, '0/3002', 1, 5, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(26, '3003', '财务费用', 'EXPENSE', 0, '', 1, '0', 0, 3, 1, '企业筹资活动发生的费用', 'admin', NOW(), '', NULL, NULL),
(27, '3003001', '利息支出', 'EXPENSE', 26, '3003', 2, '0/3003', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(28, '3003002', '手续费', 'EXPENSE', 26, '3003', 2, '0/3003', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(29, '3003003', '汇兑损失', 'EXPENSE', 26, '3003', 2, '0/3003', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),

-- 资产类科目 (ASSET)
(30, '4001', '流动资产', 'ASSET', 0, '', 1, '0', 0, 1, 1, '一年内可变现的资产', 'admin', NOW(), '', NULL, NULL),
(31, '4001001', '货币资金', 'ASSET', 30, '4001', 2, '0/4001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(32, '4001002', '应收账款', 'ASSET', 30, '4001', 2, '0/4001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(33, '4001003', '存货', 'ASSET', 30, '4001', 2, '0/4001', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(34, '4002', '固定资产', 'ASSET', 0, '', 1, '0', 0, 2, 1, '长期使用的资产', 'admin', NOW(), '', NULL, NULL),
(35, '4002001', '房屋建筑物', 'ASSET', 34, '4002', 2, '0/4002', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(36, '4002002', '机器设备', 'ASSET', 34, '4002', 2, '0/4002', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(37, '4002003', '运输工具', 'ASSET', 34, '4002', 2, '0/4002', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),

-- 负债类科目 (LIABILITY)
(38, '5001', '流动负债', 'LIABILITY', 0, '', 1, '0', 0, 1, 1, '一年内需偿还的债务', 'admin', NOW(), '', NULL, NULL),
(39, '5001001', '短期借款', 'LIABILITY', 38, '5001', 2, '0/5001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(40, '5001002', '应付账款', 'LIABILITY', 38, '5001', 2, '0/5001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(41, '5001003', '应付职工薪酬', 'LIABILITY', 38, '5001', 2, '0/5001', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(42, '5002', '长期负债', 'LIABILITY', 0, '', 1, '0', 0, 2, 1, '一年以上需偿还的债务', 'admin', NOW(), '', NULL, NULL),
(43, '5002001', '长期借款', 'LIABILITY', 42, '5002', 2, '0/5002', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(44, '5002002', '应付债券', 'LIABILITY', 42, '5002', 2, '0/5002', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),

-- 权益类科目 (EQUITY)
(45, '6001', '所有者权益', 'EQUITY', 0, '', 1, '0', 0, 1, 1, '所有者对企业净资产的所有权', 'admin', NOW(), '', NULL, NULL),
(46, '6001001', '实收资本', 'EQUITY', 45, '6001', 2, '0/6001', 1, 1, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(47, '6001002', '资本公积', 'EQUITY', 45, '6001', 2, '0/6001', 1, 2, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(48, '6001003', '盈余公积', 'EQUITY', 45, '6001', 2, '0/6001', 1, 3, 1, NULL, 'admin', NOW(), '', NULL, NULL),
(49, '6001004', '未分配利润', 'EQUITY', 45, '6001', 2, '0/6001', 1, 4, 1, NULL, 'admin', NOW(), '', NULL, NULL);

-- ----------------------------
-- 2、预算单头表
-- ----------------------------
DROP TABLE IF EXISTS `budget_sheet`;
CREATE TABLE `budget_sheet` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_no` VARCHAR(50) NOT NULL COMMENT '预算单号（唯一，格式：BG-YYYYMM-XXX）',
  `org_id` BIGINT(20) NOT NULL COMMENT '组织ID（关联sys_dept表的dept_id）',
  `org_name` VARCHAR(100) DEFAULT NULL COMMENT '组织名称（冗余字段，便于查询）',
  `budget_month` VARCHAR(7) NOT NULL COMMENT '预算月份（格式：YYYY-MM）',
  `status` VARCHAR(20) NOT NULL DEFAULT 'Draft' COMMENT '状态码（Draft/Pending_Review/Branch_Pending/Pending_Revision/Approved）',
  `reject_level` VARCHAR(10) DEFAULT 'None' COMMENT '驳回来源（HQ/Branch/None）',
  `reject_reason` TEXT COMMENT '驳回理由（最新一次的驳回理由）',
  `deadline_time` DATETIME DEFAULT NULL COMMENT '截止时间（根据驳回层级自动计算）',
  `current_handler` VARCHAR(50) DEFAULT NULL COMMENT '当前处理人（用户工号）',
  `total_budget` DECIMAL(18,2) DEFAULT 0.00 COMMENT '预算总额（自动汇总明细表）',
  `total_actual` DECIMAL(18,2) DEFAULT 0.00 COMMENT '实际总额（脱敏后数据）',
  `variance_rate` DECIMAL(5,2) DEFAULT 0.00 COMMENT '差异率（自动计算：(预算-实际)/实际*100）',
  `process_instance_id` VARCHAR(64) DEFAULT NULL COMMENT 'Flowable流程实例ID',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者（用户账号）',
  `create_time` DATETIME DEFAULT NULL COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者（用户账号）',
  `update_time` DATETIME DEFAULT NULL COMMENT '更新时间',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_sheet_no` (`sheet_no`),
  KEY `idx_org_month` (`org_id`, `budget_month`),
  KEY `idx_status` (`status`),
  KEY `idx_budget_month` (`budget_month`),
  KEY `idx_process_instance_id` (`process_instance_id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算单头表';

-- ----------------------------
-- 3、预算明细表
-- ----------------------------
DROP TABLE IF EXISTS `budget_detail`;
CREATE TABLE `budget_detail` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_id` BIGINT(20) NOT NULL COMMENT '表头ID（外键，关联budget_sheet.id）',
  `subject_code` VARCHAR(20) NOT NULL COMMENT '科目编码（关联budget_subject表）',
  `subject_name` VARCHAR(100) DEFAULT NULL COMMENT '科目名称（冗余字段）',
  `budget_amount` DECIMAL(18,2) DEFAULT 0.00 COMMENT '预算金额',
  `actual_amount` DECIMAL(18,2) DEFAULT 0.00 COMMENT '实际金额（脱敏存储，查询时根据权限决定是否显示）',
  `variance_amount` DECIMAL(18,2) DEFAULT 0.00 COMMENT '差异金额（自动计算：预算-实际）',
  `variance_rate` DECIMAL(5,2) DEFAULT 0.00 COMMENT '差异率（自动计算：(预算-实际)/实际*100）',
  `dept_id` BIGINT(20) DEFAULT NULL COMMENT '部门ID（关联sys_dept表的dept_id）',
  `dept_name` VARCHAR(100) DEFAULT NULL COMMENT '部门名称（冗余字段）',
  `sort_order` INT(4) DEFAULT 0 COMMENT '排序号（用于前端展示顺序）',
  `create_time` DATETIME DEFAULT NULL COMMENT '创建时间',
  `update_time` DATETIME DEFAULT NULL COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_sheet_id` (`sheet_id`),
  KEY `idx_subject` (`subject_code`),
  KEY `idx_dept_id` (`dept_id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算明细表';

-- ----------------------------
-- 4、驳回历史记录表
-- ----------------------------
DROP TABLE IF EXISTS `budget_reject_history`;
CREATE TABLE `budget_reject_history` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_id` BIGINT(20) NOT NULL COMMENT '预算单ID（外键，关联budget_sheet.id）',
  `sheet_no` VARCHAR(50) DEFAULT NULL COMMENT '预算单号（冗余字段）',
  `reject_from_level` VARCHAR(10) NOT NULL COMMENT '驳回方层级（HQ/Branch）',
  `reject_from_user` VARCHAR(50) NOT NULL COMMENT '驳回人工号（用户账号）',
  `reject_from_name` VARCHAR(50) DEFAULT NULL COMMENT '驳回人姓名（冗余字段）',
  `reject_to_level` VARCHAR(10) NOT NULL COMMENT '被驳回方层级（Branch/Dept）',
  `reject_to_user` VARCHAR(50) DEFAULT NULL COMMENT '被驳回人工号（用户账号，可为空）',
  `reject_to_dept_id` BIGINT(20) DEFAULT NULL COMMENT '被驳回部门ID（打回部门时必填，关联sys_dept表）',
  `reject_to_dept_name` VARCHAR(100) DEFAULT NULL COMMENT '被驳回部门名称（冗余字段）',
  `reject_reason` TEXT NOT NULL COMMENT '驳回理由',
  `deadline_time` DATETIME NOT NULL COMMENT '截止时间',
  `reject_time` DATETIME NOT NULL DEFAULT NOW() COMMENT '驳回时间',
  `handle_time` DATETIME DEFAULT NULL COMMENT '处理时间',
  `handle_duration_hours` DECIMAL(8,2) DEFAULT NULL COMMENT '处理耗时（小时，自动计算）',
  `is_timeout` TINYINT(1) DEFAULT 0 COMMENT '是否超时（0-否，1-是）',
  PRIMARY KEY (`id`),
  KEY `idx_sheet_id` (`sheet_id`),
  KEY `idx_reject_time` (`reject_time`),
  KEY `idx_reject_from_user` (`reject_from_user`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='驳回历史记录表';

-- ----------------------------
-- 5、预算风控规则表
-- ----------------------------
DROP TABLE IF EXISTS `budget_risk_rule`;
CREATE TABLE `budget_risk_rule` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `rule_code` VARCHAR(50) NOT NULL COMMENT '规则编码（唯一）',
  `rule_name` VARCHAR(100) NOT NULL COMMENT '规则名称',
  `rule_category` VARCHAR(20) NOT NULL COMMENT '规则类别（BUDGET_OVER/LOGIC_ERROR/SUBJECT_ERROR/MISSING_DATA/HISTORICAL_COMPARE）',
  `threshold_value` DECIMAL(10,2) DEFAULT NULL COMMENT '阈值',
  `risk_level` VARCHAR(10) NOT NULL DEFAULT 'MEDIUM' COMMENT '风险等级（HIGH/MEDIUM/LOW）',
  `rule_description` VARCHAR(500) DEFAULT NULL COMMENT '规则描述',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用（0-禁用，1-启用）',
  `sort_order` INT(4) DEFAULT 0 COMMENT '排序号',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者',
  `create_time` DATETIME DEFAULT NULL COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者',
  `update_time` DATETIME DEFAULT NULL COMMENT '更新时间',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_rule_code` (`rule_code`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算风控规则表';

-- ----------------------------
-- 初始化-预算风控规则表数据
-- ----------------------------
INSERT INTO `budget_risk_rule` VALUES 
(1, 'RULE_001', '预算超标检查', 'BUDGET_OVER', 20.00, 'HIGH', '部门预算与上月实际偏差超过±20%', 1, 1, 'admin', NOW(), '', NULL, NULL),
(2, 'RULE_002', '汇总逻辑检查', 'LOGIC_ERROR', NULL, 'HIGH', '明细合计与汇总金额不一致', 1, 2, 'admin', NOW(), '', NULL, NULL),
(3, 'RULE_003', '科目归类检查', 'SUBJECT_ERROR', NULL, 'MEDIUM', '科目编码不存在于科目表', 1, 3, 'admin', NOW(), '', NULL, NULL),
(4, 'RULE_004', '关键指标完整性检查', 'MISSING_DATA', NULL, 'MEDIUM', '必填字段存在空值', 1, 4, 'admin', NOW(), '', NULL, NULL),
(5, 'RULE_005', '历史数据对比', 'HISTORICAL_COMPARE', 30.00, 'LOW', '本期与上期同期波动超过±30%', 1, 5, 'admin', NOW(), '', NULL, NULL);

-- ----------------------------
-- 6、驳回理由库表
-- ----------------------------
DROP TABLE IF EXISTS `budget_reject_reason`;
CREATE TABLE `budget_reject_reason` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `reason_code` VARCHAR(50) NOT NULL COMMENT '理由编码（唯一）',
  `reason_content` VARCHAR(500) NOT NULL COMMENT '理由内容',
  `reject_level` VARCHAR(10) NOT NULL COMMENT '驳回层级（HQ/Branch）',
  `category` VARCHAR(50) DEFAULT NULL COMMENT '分类',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用（0-禁用，1-启用）',
  `sort_order` INT(4) DEFAULT 0 COMMENT '排序号',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者',
  `create_time` DATETIME DEFAULT NULL COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者',
  `update_time` DATETIME DEFAULT NULL COMMENT '更新时间',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_reason_code` (`reason_code`),
  KEY `idx_reject_level` (`reject_level`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='驳回理由库表';

-- ----------------------------
-- 初始化-驳回理由库表数据
-- ----------------------------
INSERT INTO `budget_reject_reason` VALUES 
(1, 'HQ_001', '全辖超标：整体预算超出公司年度预算配额', 'HQ', '预算超标', 1, 1, 'admin', NOW(), '', NULL, NULL),
(2, 'HQ_002', '汇总逻辑错：分公司汇总数据与部门明细不一致', 'HQ', '逻辑错误', 1, 2, 'admin', NOW(), '', NULL, NULL),
(3, 'HQ_003', '科目归类不符：预算科目使用错误', 'HQ', '科目错误', 1, 3, 'admin', NOW(), '', NULL, NULL),
(4, 'HQ_004', '数据偏差超±20%：与上月实际数据偏差过大', 'HQ', '数据偏差', 1, 4, 'admin', NOW(), '', NULL, NULL),
(5, 'HQ_005', '关键指标缺失：缺少必要的支撑材料或说明', 'HQ', '数据缺失', 1, 5, 'admin', NOW(), '', NULL, NULL),
(6, 'BR_001', '部门数据异常：某部门数据明显偏离正常范围', 'Branch', '数据异常', 1, 1, 'admin', NOW(), '', NULL, NULL),
(7, 'BR_002', '科目错：预算科目选择错误', 'Branch', '科目错误', 1, 2, 'admin', NOW(), '', NULL, NULL),
(8, 'BR_003', '计算错：公式计算错误或手工录入错误', 'Branch', '计算错误', 1, 3, 'admin', NOW(), '', NULL, NULL),
(9, 'BR_004', '超配额：超出分公司分配给部门的预算配额', 'Branch', '预算超标', 1, 4, 'admin', NOW(), '', NULL, NULL),
(10, 'BR_005', '缺支撑材料：缺少必要的附件或说明文档', 'Branch', '数据缺失', 1, 5, 'admin', NOW(), '', NULL, NULL);

-- ----------------------------
-- 7、预算操作日志表
-- ----------------------------
DROP TABLE IF EXISTS `budget_operation_log`;
CREATE TABLE `budget_operation_log` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_id` BIGINT(20) NOT NULL COMMENT '预算单ID（关联budget_sheet表）',
  `sheet_no` VARCHAR(50) DEFAULT NULL COMMENT '预算单号（冗余字段）',
  `operation_type` VARCHAR(20) NOT NULL COMMENT '操作类型（CREATE/SUBMIT/APPROVE/REJECT/SEND_BACK/UPDATE/DELETE）',
  `operator_user` VARCHAR(50) NOT NULL COMMENT '操作人（用户账号）',
  `operator_name` VARCHAR(50) DEFAULT NULL COMMENT '操作人姓名（冗余字段）',
  `operator_dept_id` BIGINT(20) DEFAULT NULL COMMENT '操作人部门ID（关联sys_dept表）',
  `operator_ip` VARCHAR(128) DEFAULT NULL COMMENT '操作IP地址',
  `old_status` VARCHAR(20) DEFAULT NULL COMMENT '原状态',
  `new_status` VARCHAR(20) DEFAULT NULL COMMENT '新状态',
  `operation_desc` VARCHAR(500) DEFAULT NULL COMMENT '操作描述',
  `operation_time` DATETIME NOT NULL DEFAULT NOW() COMMENT '操作时间',
  PRIMARY KEY (`id`),
  KEY `idx_sheet_id` (`sheet_id`),
  KEY `idx_operator_user` (`operator_user`),
  KEY `idx_operation_time` (`operation_time`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算操作日志表';

-- ----------------------------
-- 8、预算邮件发送日志表
-- ----------------------------
DROP TABLE IF EXISTS `budget_email_log`;
CREATE TABLE `budget_email_log` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_id` BIGINT(20) NOT NULL COMMENT '预算单ID（关联budget_sheet表）',
  `sheet_no` VARCHAR(50) DEFAULT NULL COMMENT '预算单号（冗余字段）',
  `email_type` VARCHAR(20) NOT NULL COMMENT '邮件类型（REJECT/SEND_BACK/TIMEOUT_REMIND）',
  `recipient_user` VARCHAR(50) NOT NULL COMMENT '收件人（用户账号）',
  `recipient_email` VARCHAR(100) NOT NULL COMMENT '收件人邮箱',
  `email_subject` VARCHAR(200) NOT NULL COMMENT '邮件主题',
  `send_status` VARCHAR(10) NOT NULL DEFAULT 'PENDING' COMMENT '发送状态（PENDING/SUCCESS/FAILED）',
  `error_message` TEXT COMMENT '错误信息（发送失败时记录）',
  `retry_count` INT(2) DEFAULT 0 COMMENT '重试次数',
  `send_time` DATETIME DEFAULT NULL COMMENT '发送时间',
  `create_time` DATETIME NOT NULL DEFAULT NOW() COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_sheet_id` (`sheet_id`),
  KEY `idx_recipient_user` (`recipient_user`),
  KEY `idx_send_time` (`send_time`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='预算邮件发送日志表';

SET FOREIGN_KEY_CHECKS = 1;
