# 企业全面预算管理系统 — 详细设计文档

**版本**：v1.2  
**日期**：2026-07-03  
**项目**：基于 RuoYi-Flowable-Plus 框架的企业级预算管理解决方案  
**文档定位**：供 AI Agent / 开发者按本文档逐模块实现代码

| 角色分类 | 对应员工编号 |
|---------|----------|
| 部门预算编制人员 | H00889 |
| 部门经理 | H00998 |
| 分公司本部 | H00223 |
| 总公司本部 | H00001 |

---

## 一、设计概述

### 1.1 文档目的

本文档是预算编制审核系统的详细设计文档，面向 AI Agent 开发场景。文档按照**模块→文件→类→方法**的粒度组织，每个待实现的功能点都给出了：
- 文件路径与类名
- 方法签名（入参、返回值）
- 核心业务逻辑伪代码
- 依赖关系与实现顺序

Agent 可直接按照本文档的章节顺序，逐个任务完成代码实现。

### 1.2 已完成 vs 待实现

| 模块 | 已完成 | 待实现 |
|------|--------|--------|
| Entity/BO/VO | ✅ BudgetPreparation, BudgetPreparationDetail, BudgetSubject | ⏳ BudgetRejectHistory, BudgetValidationRule（实体已存在但字段与DB不匹配，待修复） |
| Mapper | ✅ BudgetPreparationMapper, BudgetPreparationDetailMapper, BudgetSubjectMapper | ⏳ BudgetRejectHistoryMapper, BudgetValidationRuleMapper |
| Service | ✅ BudgetPreparationServiceImpl（基础CRUD+状态流转+完成编制+审批权限校验） | ⏳ 风控校验完整实现, 邮件通知 |
| Controller | ✅ BudgetPreparationController（15个端点，含完成编制） | ⏳ 打回部门接口, 驳回历史查询接口 |
| 前端 | ✅ index.vue, wizard.vue, approval.vue, approvalDetail.vue | ⏳ 倒计时组件, 驳回历史时间轴 |
| 前端已优化 | ✅ 向导3步流程、编辑模式直接进入第2步、部门+月份重复校验、BigDecimal安全转换、budgetPeriod计算列组合显示、Completed完成编制状态、审批权限角色-阶段匹配、驳回原因展示 | — |
| 数据库 | ✅ budget_preparation, budget_preparation_detail, budget_validation_rule, budget_subject | ⏳ budget_reject_history |
| Flowable | ✅ 三级审批流程集成（BPMN + 启动/完成/回退）、双字段状态模型 | 流程变量优化 |

### 1.3 实现顺序（Agent 按此顺序开发）

```
Phase 1: 数据库补全
  └─ Task 1.1: 创建 budget_reject_history 表

Phase 2: 后端缺失实体/Mapper
  └─ Task 2.1: BudgetRejectHistory 全栈（Entity→BO→VO→Mapper→XML）
  └─ Task 2.2: BudgetValidationRule 全栈（Entity→VO→Mapper→XML）

Phase 3: 后端业务逻辑补全
  └─ Task 3.1: 完善风控校验服务（getValidationResults 完整实现）
  └─ Task 3.2: 驳回历史记录服务（reject 时同步写入 history）
  └─ Task 3.3: 打回部门接口（sendBack）
  └─ Task 3.4: 预算总额自动汇总（明细保存后更新主表）
  └─ Task 3.5: 提交审核时的明细完整性校验

Phase 4: 邮件通知服务
  └─ Task 4.1: 邮件模板配置
  └─ Task 4.2: NotificationService 实现
  └─ Task 4.3: Quartz 定时任务（超时提醒）

Phase 5: Flowable 工作流集成（✅ 已完成）
  └─ Task 5.1: ✅ BPMN 三级审批流程定义文件
  └─ Task 5.2: ✅ 提交审核时启动流程
  └─ Task 5.3: ✅ 审批通过/驳回时完成流程任务

Phase 5.5: 系统消息通知服务
  └─ Task 5.5.1: 创建 sys_message 表
  └─ Task 5.5.2: SysMessage 全栈（Entity→VO→Mapper→XML）
  └─ Task 5.5.3: ISysMessageService 实现（未读数/列表/标记已读）
  └─ Task 5.5.4: 前端铃铛组件 + 消息下拉面板

Phase 6: 前端补全
  └─ Task 6.1: 倒计时组件
  └─ Task 6.2: 驳回历史时间轴
  └─ Task 6.3: 风控校验实时提示
```

---

## 二、Phase 1：数据库补全

### Task 1.1：创建 budget_reject_history 表

**文件**：`script/sql/mysql/budget/budget_reject_history.sql`

**SQL**：
```sql
CREATE TABLE `budget_reject_history` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `sheet_id` BIGINT(20) NOT NULL COMMENT '预算单ID',
  `sheet_no` VARCHAR(50) DEFAULT NULL COMMENT '预算单号',
  `reject_from_level` VARCHAR(10) NOT NULL COMMENT '驳回方层级(HQ/Branch)',
  `reject_from_user` VARCHAR(50) DEFAULT NULL COMMENT '驳回人工号',
  `reject_from_name` VARCHAR(50) DEFAULT NULL COMMENT '驳回人姓名',
  `reject_to_level` VARCHAR(10) NOT NULL COMMENT '被驳回方层级(Branch/Dept)',
  `reject_to_user` VARCHAR(50) DEFAULT NULL COMMENT '被驳回人工号',
  `reject_to_dept_id` BIGINT(20) DEFAULT NULL COMMENT '被驳回部门ID',
  `reject_to_dept_name` VARCHAR(100) DEFAULT NULL COMMENT '被驳回部门名称',
  `reject_reason` TEXT NOT NULL COMMENT '驳回理由',
  `deadline_time` DATETIME DEFAULT NULL COMMENT '截止时间',
  `reject_time` DATETIME NOT NULL COMMENT '驳回时间',
  `handle_time` DATETIME DEFAULT NULL COMMENT '处理时间',
  `handle_duration_hours` DECIMAL(8,2) DEFAULT NULL COMMENT '处理耗时(小时)',
  `is_timeout` TINYINT(1) DEFAULT 0 COMMENT '是否超时(0否1是)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_sheet_id` (`sheet_id`),
  KEY `idx_reject_time` (`reject_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预算驳回历史记录表';
```

---

## 三、Phase 2：后端缺失实体/Mapper

### Task 2.1：BudgetRejectHistory 全栈

**Agent 实现规范**：严格遵循现有 `BudgetPreparation` 的代码模式。

#### 2.1.1 Entity

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/domain/BudgetRejectHistory.java`

**规范**：
```java
package com.ruoyi.system.domain;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("budget_reject_history")
public class BudgetRejectHistory extends BaseEntity {
    @TableId(value = "id")
    private Long id;
    private Long sheetId;
    private String sheetNo;
    private String rejectFromLevel;   // HQ / Branch
    private String rejectFromUser;
    private String rejectFromName;
    private String rejectToLevel;     // Branch / Dept
    private String rejectToUser;
    private Long rejectToDeptId;
    private String rejectToDeptName;
    private String rejectReason;
    private Date deadlineTime;
    private Date rejectTime;
    private Date handleTime;
    private BigDecimal handleDurationHours;
    private Integer isTimeout;        // 0/1
}
```

**模式参考**：与 `BudgetPreparation.java` 一致，继承 `BaseEntity`，使用 `@TableName` + `@TableId`。

#### 2.1.2 BO

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/domain/bo/BudgetRejectHistoryBo.java`

**规范**：
```java
@Data
public class BudgetRejectHistoryBo extends BaseEntity {
    @NotNull(message = "预算单ID不能为空", groups = {AddGroup.class})
    private Long sheetId;
    private String sheetNo;
    @NotBlank(message = "驳回方层级不能为空", groups = {AddGroup.class})
    private String rejectFromLevel;
    private String rejectFromUser;
    private String rejectFromName;
    @NotBlank(message = "被驳回方层级不能为空", groups = {AddGroup.class})
    private String rejectToLevel;
    private String rejectToUser;
    private Long rejectToDeptId;
    private String rejectToDeptName;
    @NotBlank(message = "驳回理由不能为空", groups = {AddGroup.class})
    private String rejectReason;
    private Date deadlineTime;
    // 不需要 @NotNull，由系统自动填充
    private Date rejectTime;
    private Date handleTime;
    private BigDecimal handleDurationHours;
    private Integer isTimeout;
}
```

#### 2.1.3 VO

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/domain/vo/BudgetRejectHistoryVo.java`

**规范**：
```java
@Data
@ExcelIgnoreUnannotated
public class BudgetRejectHistoryVo {
    @TableId private Long id;
    @ExcelProperty("预算单ID") private Long sheetId;
    @ExcelProperty("预算单号") private String sheetNo;
    @ExcelProperty("驳回方层级") private String rejectFromLevel;
    @ExcelProperty("驳回人") private String rejectFromName;
    @ExcelProperty("被驳回方层级") private String rejectToLevel;
    @ExcelProperty("被驳回部门") private String rejectToDeptName;
    @ExcelProperty("驳回理由") private String rejectReason;
    @ExcelProperty("截止时间") private Date deadlineTime;
    @ExcelProperty("驳回时间") private Date rejectTime;
    @ExcelProperty("处理时间") private Date handleTime;
    @ExcelProperty("处理耗时(小时)") private BigDecimal handleDurationHours;
    @ExcelProperty("是否超时") private Integer isTimeout;
}
```

#### 2.1.4 Mapper

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/mapper/BudgetRejectHistoryMapper.java`

```java
public interface BudgetRejectHistoryMapper extends BaseMapperPlus<BudgetRejectHistoryMapper, BudgetRejectHistory, BudgetRejectHistoryVo> {
}
```

#### 2.1.5 Mapper XML

**文件**：`ruoyi-system/src/main/resources/mapper/system/BudgetRejectHistoryMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BudgetRejectHistoryMapper">
    <resultMap id="BudgetRejectHistoryResult" type="com.ruoyi.system.domain.BudgetRejectHistory">
        <id property="id" column="id"/>
        <result property="sheetId" column="sheet_id"/>
        <result property="sheetNo" column="sheet_no"/>
        <result property="rejectFromLevel" column="reject_from_level"/>
        <result property="rejectFromUser" column="reject_from_user"/>
        <result property="rejectFromName" column="reject_from_name"/>
        <result property="rejectToLevel" column="reject_to_level"/>
        <result property="rejectToUser" column="reject_to_user"/>
        <result property="rejectToDeptId" column="reject_to_dept_id"/>
        <result property="rejectToDeptName" column="reject_to_dept_name"/>
        <result property="rejectReason" column="reject_reason"/>
        <result property="deadlineTime" column="deadline_time"/>
        <result property="rejectTime" column="reject_time"/>
        <result property="handleTime" column="handle_time"/>
        <result property="handleDurationHours" column="handle_duration_hours"/>
        <result property="isTimeout" column="is_timeout"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
</mapper>
```

---

### Task 2.2：BudgetValidationRule 全栈

#### 2.2.1 Entity

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/domain/BudgetValidationRule.java`

```java
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("budget_validation_rule")
public class BudgetValidationRule extends BaseEntity {
    @TableId(value = "id")
    private Long id;
    private String ruleCode;           // VAL_001
    private String ruleName;           // 预算超标检查
    private String ruleCategory;       // BUDGET_OVER / LOGIC_ERROR / SUBJECT_ERROR / MISSING_DATA / HISTORICAL_COMPARE / FORMULA_CHECK
    private BigDecimal thresholdValue; // 阈值（如 20.00 表示 20%）
    private String riskLevel;          // HIGH / MEDIUM / LOW
    private String validationScope;    // ALL / INCOME / COST / EXPENSE / ASSET / LIABILITY / EQUITY
    private Integer isStrict;          // 1=严格(阻止提交) / 0=仅警告
    private String ruleFormula;        // 校验公式描述
    private String ruleDescription;    // 规则描述
    private String errorMessage;       // 错误提示模板
    private Integer isActive;          // 1=启用 / 0=禁用
}
```

#### 2.2.2 VO

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/domain/vo/BudgetValidationRuleVo.java`

与 Entity 字段一一对应，添加 `@ExcelProperty` 注解。

#### 2.2.3 Mapper

**文件**：`ruoyi-system/src/main/java/com/ruoyi/system/mapper/BudgetValidationRuleMapper.java`

```java
public interface BudgetValidationRuleMapper extends BaseMapperPlus<BudgetValidationRuleMapper, BudgetValidationRule, BudgetValidationRuleVo> {
}
```

---

## 四、Phase 3：后端业务逻辑补全

### Task 3.1：完善风控校验服务

**修改文件**：`BudgetPreparationServiceImpl.java` 的 `getValidationResults` 方法

**当前状态**：返回空结果（TODO）

**目标实现**：

```java
@Override
public List<ValidationResultVo> getValidationResults(Long sheetId) {
    List<ValidationResultVo> results = new ArrayList<>();
    
    // 1. 获取启用的校验规则
    List<BudgetValidationRule> rules = validationRuleMapper.selectVoList(
        Wrappers.<BudgetValidationRule>lambdaQuery()
            .eq(BudgetValidationRule::getIsActive, 1)
    );
    
    // 2. 获取预算明细
    List<BudgetPreparationDetail> details = detailMapper.selectList(
        Wrappers.<BudgetPreparationDetail>lambdaQuery()
            .eq(BudgetPreparationDetail::getSheetId, sheetId)
    );
    
    // 3. 获取主表数据
    BudgetPreparation sheet = baseMapper.selectById(sheetId);
    
    // 4. 逐条规则执行校验
    for (BudgetValidationRule rule : rules) {
        List<ValidationResultVo> ruleResults = executeRule(rule, details, sheet);
        results.addAll(ruleResults);
    }
    
    return results;
}
```

**校验规则执行逻辑**：

```java
private List<ValidationResultVo> executeRule(BudgetValidationRule rule, 
    List<BudgetPreparationDetail> details, BudgetPreparation sheet) {
    List<ValidationResultVo> results = new ArrayList<>();
    
    switch (rule.getRuleCategory()) {
        case "BUDGET_OVER":
            // 检查每条明细的偏差是否超过阈值
            for (BudgetPreparationDetail d : details) {
                if (d.getActualAmount() != null && d.getActualAmount().compareTo(BigDecimal.ZERO) > 0) {
                    BigDecimal rate = d.getVarianceAmount().abs()
                        .divide(d.getActualAmount().abs(), 4, RoundingMode.HALF_UP)
                        .multiply(new BigDecimal("100"));
                    if (rate.compareTo(rule.getThresholdValue()) > 0) {
                        results.add(buildResult(rule, d, "偏差" + rate + "%超过阈值" + rule.getThresholdValue() + "%"));
                    }
                }
            }
            break;
        case "LOGIC_ERROR":
            // 明细合计 vs 主表总额
            BigDecimal detailSum = details.stream()
                .map(BudgetPreparationDetail::getBudgetAmount)
                .reduce(BigDecimal.ZERO, BigDecimal::add);
            if (detailSum.compareTo(sheet.getTotalBudget()) != 0) {
                results.add(buildResult(rule, null, "明细合计" + detailSum + "与总额" + sheet.getTotalBudget() + "不一致"));
            }
            break;
        case "SUBJECT_ERROR":
            // 检查科目编码是否存在
            for (BudgetPreparationDetail d : details) {
                long count = subjectMapper.selectCount(
                    Wrappers.<BudgetSubject>lambdaQuery()
                        .eq(BudgetSubject::getSubjectCode, d.getSubjectCode())
                        .eq(BudgetSubject::getIsActive, 1));
                if (count == 0) {
                    results.add(buildResult(rule, d, "科目编码" + d.getSubjectCode() + "不存在"));
                }
            }
            break;
        case "MISSING_DATA":
            // 检查必填字段
            for (BudgetPreparationDetail d : details) {
                if (d.getBudgetAmount() == null || d.getSubjectCode() == null) {
                    results.add(buildResult(rule, d, "必填字段缺失"));
                }
            }
            break;
        case "HISTORICAL_COMPARE":
            // 与上期对比（需要查询上月数据，此处为框架代码）
            // TODO: 查询上月同期明细，计算波动率
            break;
        case "FORMULA_CHECK":
            // 收入类科目金额 >= 0
            for (BudgetPreparationDetail d : details) {
                if ("INCOME".equals(d.getSubjectType()) && d.getBudgetAmount().compareTo(BigDecimal.ZERO) < 0) {
                    results.add(buildResult(rule, d, "收入类科目金额不能为负"));
                }
            }
            break;
    }
    return results;
}
```

**新增依赖注入**：
```java
private final BudgetValidationRuleMapper validationRuleMapper;
private final BudgetPreparationDetailMapper detailMapper;
private final BudgetSubjectMapper subjectMapper;
```

---

### Task 3.2：驳回历史记录服务

**修改文件**：`BudgetPreparationServiceImpl.java` 的 `reject` 方法

**目标**：在驳回操作时，同步写入 `budget_reject_history` 表

**注意**：三级审批流程下，任何级别驳回都直接回退到编制人员，状态变为 Rejected（双字段设计下 status=Rejected, approval_stage=None）

```java
@Override
@Transactional(rollbackFor = Exception.class)
public Boolean reject(Long id, String reason) {
    // ... 现有校验逻辑保持不变 ...
    
    // 更新主表：任何级别驳回都回退到 Rejected
    preparation.setStatus("Rejected");
    preparation.setApprovalStage("None");
    preparation.setRejectReason(reason);
    // 根据当前审批级别确定驳回来源
    preparation.setRejectLevel(getCurrentApprovalLevel(preparation));
    preparation.setCurrentHandler(LoginHelper.getUsername());
    boolean result = baseMapper.updateById(preparation) > 0;
    
    // ★ 新增：写入驳回历史
    if (result) {
        BudgetRejectHistory history = new BudgetRejectHistory();
        history.setSheetId(id);
        history.setSheetNo(preparation.getSheetNo());
        history.setRejectFromLevel(getCurrentApprovalLevel(preparation));
        history.setRejectFromUser(LoginHelper.getUsername());
        history.setRejectFromName(LoginHelper.getNickName());
        history.setRejectToLevel("Dept"); // 驳回给编制人员
        history.setRejectReason(reason);
        history.setRejectTime(new Date());
        // 截止时间：次月3日
        history.setDeadlineTime(calculateDeadline());
        rejectHistoryMapper.insert(history);
    }
    
    return result;
}
```

**截止时间计算工具方法**：
```java
private Date calculateDeadline() {
    Calendar cal = Calendar.getInstance();
    cal.add(Calendar.MONTH, 1); // 次月
    cal.set(Calendar.DAY_OF_MONTH, 3); // 次月3日截止
    cal.set(Calendar.HOUR_OF_DAY, 18); // 18:00
    cal.set(Calendar.MINUTE, 0);
    cal.set(Calendar.SECOND, 0);
    return cal.getTime();
}
```

---

### Task 3.3：打回部门接口

> **注意**：三级审批流程下，驳回统一由 reject 方法处理（任何级别驳回都直接回退到编制人员）。打回部门接口作为分公司自行重编场景的补充。

**新增方法**：`IBudgetPreparationService` 接口添加 `sendBackToDept`

**Service 接口**：
```java
/**
 * 分公司打回至部门
 * @param id 预算单ID
 * @param deptId 目标部门ID
 * @param reason 打回理由
 */
Boolean sendBackToDept(Long id, Long deptId, String reason);
```

**Service 实现**：
```java
@Override
@Transactional(rollbackFor = Exception.class)
public Boolean sendBackToDept(Long id, Long deptId, String reason) {
    BudgetPreparation preparation = baseMapper.selectById(id);
    if (preparation == null) throw new ServiceException("预算编制不存在");
    
    // 校验状态：必须是 Pending_Review + approval_stage=Branch
    if (!"Pending_Review".equals(preparation.getStatus()) 
        || !"Branch".equals(preparation.getApprovalStage())) {
        throw new ServiceException("只有分公司待审核状态才能打回部门");
    }
    if (StringUtils.isBlank(reason)) {
        throw new ServiceException("打回理由不能为空");
    }
    
    // 更新状态：回退到 Rejected，编制人员可修改后重新提交
    preparation.setStatus("Rejected");
    preparation.setApprovalStage("None");
    preparation.setRejectLevel("Branch");
    preparation.setRejectReason(reason);
    preparation.setCurrentHandler(LoginHelper.getUsername());
    boolean result = baseMapper.updateById(preparation) > 0;
    
    // 写入驳回历史
    if (result) {
        BudgetRejectHistory history = new BudgetRejectHistory();
        history.setSheetId(id);
        history.setSheetNo(preparation.getSheetNo());
        history.setRejectFromLevel("Branch");
        history.setRejectFromUser(LoginHelper.getUsername());
        history.setRejectFromName(LoginHelper.getNickName());
        history.setRejectToLevel("Dept");
        history.setRejectToDeptId(deptId);
        history.setRejectReason(reason);
        history.setRejectTime(new Date());
        history.setDeadlineTime(calculateDeadline());
        rejectHistoryMapper.insert(history);
    }
    
    return result;
}
```

**Controller 端点**：
```java
/**
 * 打回至部门（分公司操作）
 */
@SaCheckPermission("system:preparation:reject")
@Log(title = "预算编制", businessType = BusinessType.UPDATE)
@PostMapping("/sendBack/{id}")
public R<Void> sendBack(@NotNull @PathVariable Long id,
                        @NotNull @RequestParam Long deptId,
                        @RequestParam String reason) {
    return toAjax(budgetPreparationService.sendBackToDept(id, deptId, reason));
}
```

---

### Task 3.4：预算总额自动汇总

**当前状态**：✅ 已实现（前端侧）

**实现方式**：前端向导第2步完成后，在 `handleFinish()` 方法中计算 `totalBudget`，调用 `updatePreparation` 接口更新主表。

```javascript
// wizard.vue handleFinish()
await updatePreparation({
  id: this.preparationData.id,
  totalBudget: this.totalBudget,
  orgId: this.basicForm.orgId,
  budgetYear: parseInt(this.basicForm.budgetYear),
  budgetMonth: this.basicForm.budgetMonth
});
```

> 注：后端 `BudgetPreparationDetailServiceImpl.batchSave` 中的自动汇总逻辑仍可作为服务端双保险，待后续实现。

---

### Task 3.5：提交审核时的明细完整性校验

**修改文件**：`BudgetPreparationServiceImpl.java` 的 `submitForReview` 方法

**在 TODO 注释处添加**：
```java
// 校验明细数据完整性
List<BudgetPreparationDetail> details = detailMapper.selectList(
    Wrappers.<BudgetPreparationDetail>lambdaQuery()
        .eq(BudgetPreparationDetail::getSheetId, id));

if (details == null || details.isEmpty()) {
    throw new ServiceException("预算编制明细为空，请先填写预算数据");
}

// 校验是否存在金额为空的科目
boolean hasEmptyAmount = details.stream()
    .anyMatch(d -> d.getBudgetAmount() == null);
if (hasEmptyAmount) {
    throw new ServiceException("存在未填写预算金额的科目，请补充完整");
}
```

---

## 五、Phase 4：邮件通知服务

### Task 4.1：邮件模板配置

**新增文件**：`ruoyi-system/src/main/resources/templates/budget/`

| 模板文件 | 用途 |
|---------|------|
| reject-notify.html | 驳回通知 |
| sendback-notify.html | 打回通知 |
| timeout-remind.html | 超时提醒 |

**模板变量**：`{receiver_name}`, `{sheet_no}`, `{reject_reason}`, `{deadline_time}`, `{system_url}`

### Task 4.2：NotificationService

**新增文件**：
- `ruoyi-system/src/main/java/com/ruoyi/system/service/INotificationService.java`
- `ruoyi-system/src/main/java/com/ruoyi/system/service/impl/NotificationServiceImpl.java`

**接口定义**：
```java
public interface INotificationService {
    /** 发送驳回通知 */
    void sendRejectNotify(String sheetNo, String receiverName, String reason, Date deadline);
    /** 发送打回通知 */
    void sendSendBackNotify(String sheetNo, String receiverName, String reason, Date deadline);
    /** 发送超时提醒 */
    void sendTimeoutRemind(String sheetNo, String receiverName, int remainingHours, Date deadline);
}
```

**实现要点**：
- 使用 `@Async` 异步发送
- 使用 Spring `JavaMailSender` 发送邮件
- 使用 Thymeleaf 渲染 HTML 模板
- 发送失败重试 3 次，间隔 5 分钟（`@Retryable`）
- 记录发送日志到 `sys_oper_log`

### Task 4.3：Quartz 超时监控任务

**新增文件**：`ruoyi-job/src/main/java/com/ruoyi/job/service/BudgetTimeoutMonitorJob.java`

```java
@Component("budgetTimeoutMonitorJob")
public class BudgetTimeoutMonitorJob {
    /**
     * 每分钟执行一次，检查即将超时和已超时的预算单
     * - 截止前4小时：发送提醒
     * - 截止前1小时：发送紧急提醒
     * - 已超时：发送告警给上级 + 更新 is_timeout 标记
     */
    public void execute() {
        // 1. 查询 Branch_Pending 和 Pending_Revision 状态的预算单
        // 2. 计算距离截止时间的剩余小时数
        // 3. 根据阈值发送对应邮件
        // 4. 超时的更新 budget_reject_history.is_timeout = 1
    }
}
```

---

## 六、Phase 5：Flowable 工作流集成

### Task 5.1：BPMN 三级审批流程定义（✅ 已完成）

**流程结构**：
```
StartEvent → UserTask(部门经理审批) → UserTask(分公司本部审批) → UserTask(总公司本部审批) → EndEvent
```

每级审批节点后配置排他网关：
- `${result=='pass'}` → 下一节点
- `${result=='reject'}` → 退回编制人

| 节点 | ID | 候选组 | formKey |
|------|------|--------|--------|
| 部门经理审批 | deptLeaderReview | ROLE3 | budget_approval_form |
| 分公司本部审批 | branchLeaderReview | ROLE4 | budget_approval_form |
| 总公司本部审批 | hqLeaderReview | ROLE5 | budget_approval_form |

### Task 5.2：提交审核启动流程

**修改**：`BudgetPreparationServiceImpl.submitForReview`

```java
// 启动 Flowable 流程
Map<String, Object> variables = new HashMap<>();
variables.put("createBy", LoginHelper.getUsername());
variables.put("deptId", preparation.getOrgId());
variables.put("sheetNo", preparation.getSheetNo());

ProcessInstance instance = runtimeService.startProcessInstanceByKey(
    "budgetProcess", preparation.getSheetNo(), variables);

preparation.setProcessInstanceId(instance.getId());
```

### Task 5.3：审批完成/回退流程

**审批通过时**：
```java
// 完成当前任务
Task task = taskService.createTaskQuery()
    .processInstanceId(preparation.getProcessInstanceId())
    .singleResult();
if (task != null) {
    Map<String, Object> vars = Map.of("result", "pass");
    taskService.complete(task.getId(), vars);
}
```

**驳回时**：
```java
// 回退流程到编制人
runtimeService.createChangeActivityStateBuilder()
    .processInstanceId(preparation.getProcessInstanceId())
    .moveActivityIdTo(currentActivityId, "deptBudgetPrepare")
    .changeState();
```

---

## 七、Phase 6：前端补全

### Task 6.1：倒计时组件

**新增文件**：`ruoyi-ui/src/components/BudgetCountdown/index.vue`

**Props**：
| 属性 | 类型 | 说明 |
|------|------|------|
| deadline | String | 截止时间 (ISO格式) |
| status | String | 当前状态 |

**行为**：
- 实时显示 `剩余 Xh Xm Xs`
- 最后 2 小时：文字变红 + 闪烁动画
- 超时后：显示「已超时」红色标签
- 触发事件：`@timeout` 通知父组件

### Task 6.2：驳回历史时间轴

**新增文件**：`ruoyi-ui/src/components/BudgetRejectTimeline/index.vue`

**数据来源**：`GET /system/preparation/reject/history/{sheetId}`

**展示**：使用 `el-timeline` 组件
- 每条记录显示：驳回方→被驳回方、理由、时间、是否超时
- 颜色区分：HQ驳回=红色，Branch驳回=橙色

### Task 6.3：风控校验实时提示

**修改文件**：`ruoyi-ui/src/views/system/preparation/wizard.vue`

**实现**：
- 在第3步确认页加载时调用 `getValidationResults(sheetId)`
- 展示校验结果表格（规则名、风险等级、科目、问题描述）
- 严格规则（is_strict=1）不通过时，禁用“提交审核”按钮

### Task 6.4：向导编制流程优化（已完成）

**修改文件**：`ruoyi-ui/src/views/system/preparation/wizard.vue`、`index.vue`

**已实现内容**：

1. **向导3步化**：原5步合并为3步（基础信息 → 科目预算明细 → 确认提交）
2. **编辑模式直接进入第2步**：`currentStep = 1`（0-indexed），隐藏「上一步」按钮
3. **部门+月份重复校验**：

```javascript
async checkDuplicate() {
  const response = await listPreparation({
    budgetYear: parseInt(this.basicForm.budgetYear),
    budgetMonth: this.basicForm.budgetMonth,
    orgId: this.basicForm.orgId
  });
  const rows = response.rows || [];
  // 排除当前记录自身
  const filtered = rows.filter(row => row.id !== this.preparationData.id);
  return filtered.length > 0;
}
```

4. **列表页 budgetPeriod 计算列处理**：前端用 `budgetYear + budgetMonth` 组合显示
5. **BigDecimal 安全转换**：`Number(scope.row.totalBudget).toFixed(2)`
6. **编辑时完整字段传递**：`saveBasicInfo` 和 `handleFinish` 中传递 orgId、orgName 等完整字段

---

## 八、数据字典

### 8.1 状态码（双字段设计）

**生命周期状态（status）**

| 值 | 说明 | 可执行操作 |
|----|------|----------|
| Draft | 草稿 | 编辑、删除、完成编制 |
| Completed | 完成编制 | 提交审核；不可编辑、不可删除 |
| Pending_Review | 待审核 | 审批通过、驳回（具体阶段由 approval_stage 决定） |
| Pending_Revision | 待修订 | 编辑、完成编制 |
| Approved | 审核通过 | 只读，预算已锁定 |
| Rejected | 已驳回 | 编辑（重置为 Draft）、完成编制 |

**审批阶段（approval_stage）**

| 值 | 说明 | 可执行操作 |
|----|------|----------|
| None | 无 | 非审核状态时默认值 |
| Dept | 部门经理审批 | 部门经理审批通过、驳回 |
| Branch | 分公司本部审批 | 分公司本部审批通过、驳回 |
| HQ | 总公司本部审批 | 总公司本部审批通过、驳回 |

### 8.2 驳回来源

| 值 | 说明 |
|----|------|
| None | 无驳回 |
| Dept | 部门经理驳回 |
| Branch | 分公司本部驳回 |
| HQ | 总公司本部驳回 |

### 8.3 科目类型

| 值 | 说明 | Tab分组 |
|----|------|--------|
| INCOME | 收入类 | 第2步 |
| COST | 成本类 | 第2步 |
| EXPENSE | 费用类 | 第2步 |
| ASSET | 资产类 | 第2步 |
| LIABILITY | 负债类 | 第2步 |
| EQUITY | 权益类 | 第2步 |

### 8.4 风险等级

| 值 | 颜色 | Element Tag |
|----|------|-------------|
| HIGH | 红色 | danger |
| MEDIUM | 橙色 | warning |
| LOW | 灰色 | info |

---

## 九、接口变更清单

### 9.1 新增接口

| 接口路径 | 方法 | 说明 | 权限 |
|---------|------|------|------|
| /system/preparation/sendBack/{id} | POST | 打回部门 | system:preparation:reject |
| /system/preparation/reject/history/{sheetId} | GET | 查询驳回历史 | system:preparation:query |
| /system/preparation/reject/reasons | GET | 获取驳回理由库 | system:preparation:query |

### 9.2 修改接口

| 接口 | 变更内容 |
|------|---------|
| POST /reject/{id} | 新增：写入 budget_reject_history |
| POST /detail/batch | 新增：保存后自动汇总 total_budget |
| POST /submit/{id} | 新增：明细完整性校验 + 启动Flowable流程 |
| GET /validation/{sheetId} | 修改：返回完整校验结果（替代空TODO） |
| GET /list | 复用：前端 checkDuplicate 调用 listPreparation 接口进行部门+月份重复校验 |

---

## 十、文件变更索引

### 10.1 新增文件清单

| 文件路径 | 说明 |
|---------|------|
| script/sql/mysql/budget/budget_reject_history.sql | 驳回历史建表SQL |
| ruoyi-system/.../domain/BudgetRejectHistory.java | 驳回历史实体 |
| ruoyi-system/.../domain/bo/BudgetRejectHistoryBo.java | 驳回历史BO |
| ruoyi-system/.../domain/vo/BudgetRejectHistoryVo.java | 驳回历史VO |
| ruoyi-system/.../domain/vo/ValidationResultVo.java | 校验结果VO |
| ruoyi-system/.../mapper/BudgetRejectHistoryMapper.java | 驳回历史Mapper |
| ruoyi-system/.../mapper/BudgetValidationRuleMapper.java | 校验规则Mapper |
| resources/mapper/system/BudgetRejectHistoryMapper.xml | 驳回历史XML |
| resources/mapper/system/BudgetValidationRuleMapper.xml | 校验规则XML |
| ruoyi-system/.../service/INotificationService.java | 通知服务接口 |
| ruoyi-system/.../service/impl/NotificationServiceImpl.java | 通知服务实现 |
| ruoyi-job/.../BudgetTimeoutMonitorJob.java | 超时监控定时任务 |
| resources/templates/budget/reject-notify.html | 驳回邮件模板 |
| resources/templates/budget/sendback-notify.html | 打回邮件模板 |
| resources/templates/budget/timeout-remind.html | 超时邮件模板 |
| ruoyi-ui/src/components/BudgetCountdown/index.vue | 倒计时组件 |
| ruoyi-ui/src/components/BudgetRejectTimeline/index.vue | 驳回历史时间轴 |

### 10.2 修改文件清单

| 文件路径 | 修改内容 |
|---------|---------|
| BudgetPreparationServiceImpl.java | ① getValidationResults完整实现 ② reject写入历史 ③ sendBackToDept ④ submit校验明细 ⑤ calculateDeadline |
| IBudgetPreparationService.java | 新增 sendBackToDept 方法签名 |
| BudgetPreparationDetailServiceImpl.java | batchSave后汇总total_budget |
| BudgetPreparationController.java | 新增 sendBack、rejectHistory 端点 |
| wizard.vue | ① 向导3步化 ② 编辑模式直接进入第2步 ③ 部门+月份重复校验 checkDuplicate ④ handleFinish完整字段传递 ⑤ 第3步添加校验结果展示 |
| index.vue | ① budgetPeriod计算列组合显示 ② BigDecimal安全转换 Number().toFixed(2) |
| approvalDetail.vue | 添加驳回历史时间轴、倒计时 |

---

## 十一、Agent 开发检查清单

Agent 完成每个 Phase 后，按此清单自检：

### Phase 1 自检
- [ ] budget_reject_history 表创建成功
- [ ] 索引正确（sheet_id, reject_time）

### Phase 2 自检
- [ ] 所有 Entity 继承 BaseEntity
- [ ] 所有 Mapper 使用 BaseMapperPlus 三泛型
- [ ] XML namespace 与 Mapper 接口全限定名一致
- [ ] 编译无错误

### Phase 3 自检
- [ ] reject 方法写入 history 表
- [ ] sendBackToDept 状态校验正确
- [ ] batchSave 后 total_budget 自动更新
- [ ] submit 时校验明细非空
- [ ] getValidationResults 返回完整校验结果

### Phase 4 自检
- [ ] 邮件发送成功（测试环境）
- [ ] @Async 异步生效
- [ ] 重试机制生效
- [ ] 定时任务每分钟执行

### Phase 5 自检
- [x] 三级审批 BPMN 流程定义已部署
- [x] 提交审核启动 Flowable 流程，状态变为 Pending_Review + approval_stage=Dept
- [x] 部门经理通过完成当前任务，approval_stage 变为 Branch
- [x] 分公司本部通过完成当前任务，approval_stage 变为 HQ
- [x] 总公司本部通过完成当前任务，status 变为 Approved
- [x] 驳回回退流程到编制人，status 变为 Rejected
- [x] process_instance_id 正确保存
- [x] 候选组配置正确（ROLE3/ROLE4/ROLE5）

### Phase 6 自检
- [ ] 倒计时组件正确显示
- [ ] 驳回历史时间轴正确渲染
- [ ] 风控校验结果展示正确
- [ ] 严格规则不通过时禁用提交按钮
- [x] 向导3步流程正常工作
- [x] 编辑模式直接进入第2步，不可返回第1步
- [x] 部门+月份重复校验生效
- [x] 列表页 budgetPeriod 组合显示、totalBudget 正确显示
- [x] 编辑时完整字段传递（orgId、orgName）
- [x] Completed 完成编制状态前端适配
- [x] 驳回原因展示（列表页 tooltip + 向导页横幅）

---

## 十二、2026-07-03 下午更新（状态模型重构 + 审批权限改造）

| 修改项 | 说明 |
|--------|------|
| 双字段状态模型 | status（生命周期）+ approval_stage（审批阶段），status 简化为 6 个值 |
| 新增 Completed 状态 | 新增 completePreparation 方法，Draft/Rejected → Completed |
| 提交审核限制 | submitForReview 仅允许 Completed 状态 |
| 删除权限收紧 | deleteWithValidByIds 仅允许 Draft 状态 |
| 编辑重置状态 | updateByBo 编辑后自动重置 status 为 Draft |
| 审批权限改造 | 审批接口移除 @SaCheckPermission，Service 层 checkCurrentUserCanApprove 角色-阶段匹配 |
| 驳回状态变更 | 驳回后 status=Rejected, approval_stage=None |
| 新增完成编制接口 | POST /system/preparation/complete/{id} |
| 驳回原因展示 | 列表页 tooltip + 向导页横幅 |
| 审核详情页优化 | 已终结状态不显示审批按钮，非当前角色仅查看 |

---

**文档版本**：v1.2  
**创建日期**：2026-07-02  
**更新日期**：2026-07-03  
**适用对象**：AI Agent / 开发者  
**前置依赖**：需求说明书.md、技术方案.md、验收指标.md
